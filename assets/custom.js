/*
	In this script you can use the following parameters:
	
	var user_id   = 1;				= The ID of the currently logged in user
	var user_type = 'developer';	= The usertype ('developer' or 'author')
	var driver    = 'publish';		= The current page in the backend (like 'publish' or 'blueprintspages')
	var action    = 'new';			= The current action (like 'new' or 'edit')
	var section   = 'banners';		= The name of the section
	var id_entry  = false;			= The ID of the entry that is edited
*/